#include <iostream>
using namespace std;
class unionset
{
public:
    int* father;
    int n;
    unionset(int n)
    {
        father = new int[n + 1];
        this->n = n;
        for(int i = 0;i <= n;++i)
        {
            father[i] = i;
        }
    }
    int find(int x)
    {
        if(father[x] == x) return x;
        return father[x];
    }
    void merge(int a, int b)
    {
        a = find(a);
        b = find(b);
        if(a == b) return;
        father[a] = b;
        return;
    }
};
int main()
{
    cout << 1 << endl;
    return 0;
}
